services:
  cli:
    build:
      context: .
      target: cli
      dockerfile: Dockerfile
    image: "server-authorization-php-local"
    volumes:
      - ./:/opt/apps/app
  bun:
    build:
      context: .
      target: bun
      dockerfile: Dockerfile
    image: "server-authorization-bun-local"
    volumes:
      - ./:/opt/apps/app
